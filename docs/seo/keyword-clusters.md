# Keyword Clusters

Updated: 2026-04-27

## Primary cluster: AI agent audit trails

Primary intent:
- Teams searching for attributed logs, action history, and evidence generation for production AI agents.

Primary landing pages:
- `/`
- `/audit-in-2-mins`
- `/features`

Supporting content:
- `/blog/audit-trails-for-ai-agents-what-soc2-actually-requires`
- `/blog/eu-ai-act-high-risk-classification`

Internal linking targets:
- Homepage hero and footer should point to `/audit-in-2-mins`, `/features`, and `/pricing`.
- Audit/compliance posts should point to `/audit-in-2-mins` and `/features`.

Gap notes:
- A dedicated `/solutions/ai-agent-audit-trails` page would give this cluster a cleaner commercial destination than the homepage.

## Primary cluster: AI agent identity and authorization

Primary intent:
- Teams searching for per-agent identity, scoped permissions, and control-plane design for multi-agent systems.

Primary landing pages:
- `/features`
- `/integrations`

Supporting content:
- `/blog/why-every-ai-agent-needs-an-identity`
- `/blog/zero-trust-for-ai-agents-beyond-the-buzzword`

Internal linking targets:
- Identity and zero-trust posts should point to `/features`, `/integrations`, and `/audit-in-2-mins`.
- `/features` should link forward to `/integrations` and `/pricing`.

Gap notes:
- A dedicated solution page for `AI agent authorization` is still missing.
- A future comparison page for `shared service accounts vs per-agent identity` would fit this cluster.

## Primary cluster: SOC 2, HIPAA, and EU AI Act for AI agents

Primary intent:
- Buyers and operators looking for framework-specific controls and evidence expectations for AI systems.

Primary landing pages:
- `/pricing`
- `/contact`
- `/audit-in-2-mins`

Supporting content:
- `/blog/audit-trails-for-ai-agents-what-soc2-actually-requires`
- `/blog/eu-ai-act-high-risk-classification`

Internal linking targets:
- Framework-heavy posts should point to `/pricing`, `/contact`, and `/audit-in-2-mins`.
- `/pricing` should point back to the framework posts as proof material.

Gap notes:
- No dedicated landing pages exist yet for:
  - `SOC 2 for AI agents`
  - `HIPAA audit logs for AI`
  - `EU AI Act logging`
- These should be the next commercial content pages after Sprint 4.

## Page map summary

Current commercial pages:
- `/` serves the broad category entry point.
- `/features` explains product capabilities.
- `/integrations` explains runtime and evidence-system fit.
- `/pricing` handles packaging and commercial qualification.
- `/audit-in-2-mins` is the strongest self-serve conversion page.

Next pages to add:
1. `/solutions/ai-agent-audit-trails`
2. `/solutions/soc-2-for-ai-agents`
3. `/solutions/hipaa-audit-logs-for-ai`
4. `/solutions/eu-ai-act-logging`

Publishing rule:
- New posts should support one of the three clusters above.
- Every new commercial page should receive links from at least two blog posts and one navigation surface.
